import { json, readJson, newId, verifyPassword, sessionCookie, sessionExpirationIso } from "../_utils.js";
export async function onRequestPost(context){const body=await readJson(context.request);const email=String(body.email||"").trim().toLowerCase();const password=String(body.password||"");const user=await context.env.DB.prepare("SELECT id,email,name,password_hash FROM users WHERE email=?").bind(email).first();if(!user)return json({error:"Invalid email or password."},401);const ok=await verifyPassword(password,user.password_hash);if(!ok)return json({error:"Invalid email or password."},401);const sessionId=newId();await context.env.DB.prepare("INSERT INTO sessions (id,user_id,expires_at) VALUES (?,?,?)").bind(sessionId,user.id,sessionExpirationIso()).run();return json({user:{id:user.id,email:user.email,name:user.name}},200,{"set-cookie":sessionCookie(sessionId)})}