import { json, readJson, requireUser } from "./_utils.js";
export async function onRequestGet(context){const {user,response}=await requireUser(context);if(response)return response;let settings=await context.env.DB.prepare("SELECT monthly_goal FROM user_settings WHERE user_id=?").bind(user.id).first();if(!settings){await context.env.DB.prepare("INSERT INTO user_settings (user_id,monthly_goal) VALUES (?,?)").bind(user.id,15).run();settings={monthly_goal:15}}return json({monthlyGoal:Number(settings.monthly_goal)||15})}
export async function onRequestPut(context){const {user,response}=await requireUser(context);if(response)return response;const body=await readJson(context.request);const monthlyGoal=Math.max(Number(body.monthlyGoal)||1,1);await context.env.DB.prepare(`INSERT INTO user_settings (user_id,monthly_goal,updated_at) VALUES (?,?,?) ON CONFLICT(user_id) DO UPDATE SET monthly_goal=excluded.monthly_goal, updated_at=excluded.updated_at`).bind(user.id,monthlyGoal,new Date().toISOString()).run();return json({monthlyGoal})}